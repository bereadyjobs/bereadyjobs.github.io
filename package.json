{
  "devDependencies": {
    "daisyui": "^5.0.35",
    "tailwindcss": "^4.1.5"
  },
  "scripts": {
    "build:css": "npx @tailwindcss/cli -i styles.css -o output.css",
    "dev:build:css": "npx tailwindcss -i ./styles.css -o ./output.css --watch"
  }
}